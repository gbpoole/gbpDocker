version: "2"

services:
  head:
    build:   ./docker-image
    hostname: ${project_name}_head
    ports:
      - "2222:22"
    volumes:
      - hosts:/hosts
  node:
    build:  ./docker-image
    command: /usr/local/gbpDocker/compose-node-start.sh
    volumes_from:
      - head

networks:
  default:
    driver: bridge

volumes:
  hosts:
